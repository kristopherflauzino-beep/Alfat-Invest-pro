import { NextResponse } from "next/server";import { processWebhook,paymentErrorStatus } from "@/lib/payments/payment-service";export const runtime="nodejs";export async function POST(request:Request){try{const body=await request.json().catch(()=>null);if(!body)return NextResponse.json({error:"Payload invalido."},{status:400});const result=await processWebhook(request,body);return NextResponse.json(result,{status:200});}catch(error){const result=paymentErrorStatus(error);return NextResponse.json({error:result.message},{status:result.status});}}
