import { NextResponse } from "next/server";import { reconcilePendingPayments,paymentErrorStatus } from "@/lib/payments/payment-service";export const runtime="nodejs";export async function GET(request:Request){const secret=process.env.CRON_SECRET;if(!secret||request.headers.get("authorization")!==`Bearer ${secret}`)return NextResponse.json({error:"Nao autorizado."},{status:401});try{return NextResponse.json(await reconcilePendingPayments());}catch(error){const result=paymentErrorStatus(error);return NextResponse.json({error:result.message},{status:result.status});}}
