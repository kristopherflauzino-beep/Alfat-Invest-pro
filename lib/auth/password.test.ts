import { describe,expect,it } from "vitest";import { createHash } from "node:crypto";import { hashPassword,needsPasswordUpgrade,passwordPolicy,verifyPassword } from "./password";describe("senhas seguras",()=>{it("gera bcrypt custo 12",async()=>{const hash=await hashPassword("SenhaMuitoForte@2026");expect(hash.startsWith("$2")).toBe(true);expect(hash.split("$")[2]).toBe("12");expect(await verifyPassword("SenhaMuitoForte@2026",hash)).toBe(true);});it("aceita hash legado uma vez e sinaliza migracao",async()=>{const hash=createHash("sha256").update("SenhaLegada@2026").digest("hex");expect(await verifyPassword("SenhaLegada@2026",hash)).toBe(true);expect(needsPasswordUpgrade(hash)).toBe(true);});it("recusa senha curta ou contendo identidade",()=>{expect(passwordPolicy("Ab1@",{})).toBeTruthy();expect(passwordPolicy("Kristopher@2026",{name:"Kristopher"})).toBeTruthy();});});
