import type { AlfatecCryptoAnalysis } from "@/lib/analysis/alfatec-crypto";
export function CryptoScoreBreakdown({ analysis }: { analysis: AlfatecCryptoAnalysis }) {
  return <div className="rounded-3xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-slate-900"><h3 className="text-lg font-black">Composicao do score</h3><p className="mt-1 text-sm text-slate-500 dark:text-slate-300">Pesos oficiais e contribuicao calculada apenas com dados disponiveis.</p><div className="mt-4 space-y-4">{analysis.pillars.map((pillar) => <div key={pillar.key}><div className="mb-1 flex flex-wrap justify-between gap-2 text-sm"><span className="font-bold">{pillar.label} <small className="text-slate-400">peso {pillar.weight}%</small></span><strong>{pillar.score === null ? "Dado indisponivel" : pillar.key === "risk" ? `${pillar.score}/100 (maior = mais risco)` : `${pillar.score}/100`}</strong></div><div className="h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-white/10"><div className="h-full rounded-full bg-cyan-500" style={{ width: `${pillar.score ?? 0}%` }} /></div><p className="mt-1 text-xs text-slate-500 dark:text-slate-400">{pillar.explanation}{pillar.contribution === null ? "" : ` Contribuicao: ${pillar.contribution.toFixed(2)} pontos.`}</p></div>)}</div></div>;
}
