export type CryptoOpportunityFilterState = { minScore: number; category: string; minMarketCap: number; minVolume: number; maxRisk: number; minimumConfidence: string };
export const defaultCryptoOpportunityFilters: CryptoOpportunityFilterState = { minScore: 0, category: "TODAS", minMarketCap: 0, minVolume: 0, maxRisk: 100, minimumConfidence: "Baixa" };
export function CryptoOpportunityFilters({ value, onChange, categories }: { value: CryptoOpportunityFilterState; onChange: (patch: Partial<CryptoOpportunityFilterState>) => void; categories: Array<{ value: string; label: string }> }) {
  const field = "h-10 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm text-slate-950 dark:border-white/10 dark:bg-slate-950 dark:text-white";
  return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3"><label className="text-xs font-bold text-slate-500">Score minimo<input className={field} type="number" min="0" max="100" value={value.minScore} onChange={(e) => onChange({ minScore: Number(e.target.value) })} /></label><label className="text-xs font-bold text-slate-500">Categoria<select className={field} value={value.category} onChange={(e) => onChange({ category: e.target.value })}><option value="TODAS">Todas</option>{categories.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label className="text-xs font-bold text-slate-500">Capitalizacao minima<input className={field} type="number" min="0" value={value.minMarketCap} onChange={(e) => onChange({ minMarketCap: Number(e.target.value) })} /></label><label className="text-xs font-bold text-slate-500">Volume 24h minimo<input className={field} type="number" min="0" value={value.minVolume} onChange={(e) => onChange({ minVolume: Number(e.target.value) })} /></label><label className="text-xs font-bold text-slate-500">Risco maximo<input className={field} type="number" min="0" max="100" value={value.maxRisk} onChange={(e) => onChange({ maxRisk: Number(e.target.value) })} /></label><label className="text-xs font-bold text-slate-500">Confianca minima<select className={field} value={value.minimumConfidence} onChange={(e) => onChange({ minimumConfidence: e.target.value })}><option value="Alta">Alta</option><option value="Media">Media</option><option value="Baixa">Baixa</option><option value="Insuficiente">Insuficiente</option></select></label></div>;
}
