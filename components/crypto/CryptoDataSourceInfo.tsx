import { ExternalLink } from "lucide-react";
import type { AlfatecCryptoAnalysis } from "@/lib/analysis/alfatec-crypto";
export function CryptoDataSourceInfo({ analysis }: { analysis: AlfatecCryptoAnalysis }) {
  return <div className="rounded-3xl border border-slate-200 bg-white p-5 dark:border-white/10 dark:bg-slate-900"><h3 className="font-black">Fontes e qualidade dos dados</h3><div className="mt-3 grid gap-3 sm:grid-cols-2"><p className="rounded-2xl bg-slate-50 p-3 text-sm dark:bg-white/5"><span className="block text-xs text-slate-500">Fonte de mercado</span><strong>{analysis.snapshot.source}</strong></p><p className="rounded-2xl bg-slate-50 p-3 text-sm dark:bg-white/5"><span className="block text-xs text-slate-500">Consulta</span><strong>{new Date(analysis.snapshot.consultedAt).toLocaleString("pt-BR")}</strong></p></div><a className="mt-3 inline-flex items-center gap-2 text-sm font-bold text-cyan-600 dark:text-cyan-300" href={analysis.snapshot.sourceUrl} target="_blank" rel="noreferrer">Abrir fonte <ExternalLink className="h-4 w-4" /></a>{analysis.confidenceReasons.length > 0 && <ul className="mt-4 space-y-2 rounded-2xl bg-amber-500/10 p-3 text-xs font-semibold text-amber-700 dark:text-amber-300">{analysis.confidenceReasons.map((item) => <li key={item}>- {item}</li>)}</ul>}</div>;
}
